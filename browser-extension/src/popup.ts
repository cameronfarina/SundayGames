const statusElement = document.querySelector("[data-extension-status]");
if (statusElement !== null) {
  statusElement.textContent = "Ready. Sign in to ESPN, then return to Sunday Games to connect your league.";
}
